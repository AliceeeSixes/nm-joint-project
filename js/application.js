

// Slick carousels

$("#products-slider").slick({
    // Arrows
    arrows: true,
    prevArrow: "#products-arrow-prev",
    nextArrow: "#products-arrow-next",
    autoplay: true,
    autoplaySpeed: 3000,
    infinite: true,
    slidesToShow: 3
});